import SwiftUI

struct FavoritesScreen: View {
    var body: some View {
        NavigationStack {
            EmptyStateView(message: "Not implemented")
                .navigationTitle("Favorites")
        }
    }
}

